@model List<VideoPopup>

@{
    Layout = null;
}
<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
<h3>Video Bilgileri Listesi</h3>
<br />

<table class="table table-bordered">
    <tr>
        <th>ID</th>
        <th>Başlık</th>
        <th>Alt Başlık</th>
        <th>Açıklama</th>
        <th>VideoUrl</th>

    </tr>

    @foreach (var x in Model)
    {
        <tr>
            <td>@x.VideoPopupId</td>
            <td>@x.Title</td>
            <td>@x.SubTitle</td>
            <td>@x.Description</td>
            <td>@x.VideoUrl</td>

        </tr>

    }
</table>